<UserControl x:Class="MyPortfolio.Views.PortfolioContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyPortfolio.Views"
             xmlns:viewmodels="clr-namespace:MyPortfolio.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:PortfolioContentViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Button Width="100" 
                Height="30"
                Content="Press"
                Command="{Binding Path=UpdateUI}"
                CommandParameter="{Binding}"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"/>

        <Label Content="{Binding Path=LabelText}"
               FontSize="40"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Foreground="White"/>
    </Grid>
</UserControl>
